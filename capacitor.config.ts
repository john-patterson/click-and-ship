import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  // TODO: replace with your real reverse-DNS app id before building for iOS.
  appId: 'com.clickandship.app',
  appName: 'click-and-ship',
  webDir: 'dist',
}

export default config
